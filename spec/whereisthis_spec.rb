require "spec_helper"

describe Whereisthis do
  it "has a version number" do
    expect(Whereisthis::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
